export type Props = {
    title: string,
    subtitle?: string,
    rightIcon?: string,
    leftIcon?: string,
    onPressRightIcon?: (param?: any) => any,
    onPressLeftIcon?: (param?: any) => any,
};
